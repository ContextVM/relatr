{
  "name": "relatr",
  "version": "0.0.3",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run process-pastry --cmd \"bun run index.ts\" --env .env --example-env .env.example --port 3000",
    "mcp": "bun run src/mcp/server.ts",
    "format": "prettier --write .",
    "build": "bun build ./index.ts --outdir ./dist --target bun",
    "compile": "bun build --compile --minify --sourcemap ./index.ts --outfile relatr"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "@types/bun": "latest",
    "prettier": "^3.6.2"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@contextvm/sdk": "^0.1.37",
    "@modelcontextprotocol/sdk": "^1.20.2",
    "applesauce-core": "^4.1.0",
    "applesauce-relay": "^4.1.0",
    "nostr-social-graph": "^1.0.33",
    "nostr-tools": "^2.17.0",
    "process-pastry": "https://github.com/hzrd149/process-pastry",
    "zod": "^3.25.76"
  }
}
