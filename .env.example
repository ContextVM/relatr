# === Core Settings ===
DEFAULT_SOURCE_PUBKEY=<hex-pubkey>      # Required: Default perspective
GRAPH_BINARY_PATH=data/socialGraph.bin  # Social graph file location
DATABASE_PATH=data/relatr-v2.db         # SQLite database path for v2

# === Nostr Settings ===
NOSTR_RELAYS=wss://relay.damus.io,wss://relay.nostr.band,wss://nos.lol,wss://relay.snort.social
# Comma-separated relay URLs

# === Distance Normalization ===
DECAY_FACTOR=0.1                        # Alpha in distance formula

# === Caching ===
CACHE_TTL_SECONDS=3600                  # Cache TTL in seconds (1 hour)

# === Weighting Scheme ===
# Individual weights (optional - will use defaults if not specified)
WEIGHT_DISTANCE=0.5                     # Social distance weight (0-1)
WEIGHT_NIP05=0.15                       # NIP-05 validation weight (0-1)
WEIGHT_LIGHTNING=0.1                    # Lightning address weight (0-1)
WEIGHT_EVENT=0.1                        # Event kind 10002 weight (0-1)
WEIGHT_RECIPROCITY=0.15                 # Reciprocity weight (0-1)