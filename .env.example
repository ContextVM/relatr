# Relatr Configuration
# Copy this file to .env and adjust values as needed

# Required: Server secret key for Nostr operations
SERVER_SECRET_KEY=your_nostr_private_key_here

# Optional: Override default data directory
# DATA_DIR=/usr/src/app/data

# Optional: Override database path
# DATABASE_PATH=/usr/src/app/data/relatr.db

# Optional: Override graph binary path  
# GRAPH_BINARY_PATH=/usr/src/app/data/socialGraph.bin

# Optional: Default source pubkey for trust calculations
# DEFAULT_SOURCE_PUBKEY=6e468422dfb74a5738702a8823b9b28168abab8655faacb6853cd0ee15deee93

# Optional: Nostr relays for profile discovery
# NOSTR_RELAYS=wss://relay.damus.io,wss://relay.nostr.band,wss://relay.snort.social

# Optional: Server relays for MCP transport
# SERVER_RELAYS=wss://relay.contextvm.org

# Optional: Cache TTL in seconds (default: 604800 = 7 days)
# CACHE_TTL_SECONDS=604800

# Optional: Number of hops for social graph (default: 1)
# NUMBER_OF_HOPS=1

# Optional: Sync interval in milliseconds (default: 24 hours)
# SYNC_INTERVAL=86400000

# Optional: Cleanup interval in milliseconds (default: 7 hours)
# CLEANUP_INTERVAL=25200000

# Optional: Decay factor for trust calculations (default: 0.1)
# DECAY_FACTOR=0.1